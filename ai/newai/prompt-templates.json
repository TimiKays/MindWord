[
    {
        "name": "默认模板",
        "description": "默认提示词模板",
        "type": "默认",
        "caller": "all",
        "params": [],
        "content": "你是一个MindWord产品中的AI助手，请根据用户的需求，提供相应的帮助。"
    },
    {
        "name": "扩展子节点",
        "description": "为当前节点生成若干子节点（保持Markdown标题结构）",
        "type": "扩展",
        "caller": "AI应用",
        "params": [
            {
                "name": "{{fullPath}}",
                "description": "当前节点在导图中的完整路径（用于定位上下文）"
            },
            {
                "name": "{{siblingNodes}}",
                "description": "当前节点的同级兄弟节点列表（逗号分隔），用于参考风格和内容"
            },
            {
                "name": "{{context}}",
                "description": "包含多个 Markdown 标题行的上下文文本，用于生成子节点内容"
            }
        ],
        "content": "{{context}} "
    },
    {
        "name": "创建同级",
        "description": "为当前节点生成若干同级节点（保持Markdown标题结构）",
        "type": "扩展",
        "caller": "AI应用",
        "params": [
            {
                "name": "{{fullPath}}",
                "description": "当前节点在导图中的完整路径（用于定位上下文）"
            },
            {
                "name": "{{siblingNodes}}",
                "description": "当前节点的同级兄弟节点列表（逗号分隔），用于参考风格和内容"
            },
            {
                "name": "{{context}}",
                "description": "包含多个 Markdown 标题行的上下文文本，用于生成子节点内容"
            }
        ],
        "content": "{{context}} "
    },
    {
        "name": "扩写备注",
        "description": "为当前节点内容更详实的备注/正文段落。",
        "type": "扩展",
        "caller": "AI应用",
        "params": [
            {
                "name": "{{fullPath}}",
                "description": "当前节点在导图中的完整路径（用于定位上下文）"
            },
            {
                "name": "{{siblingNodes}}",
                "description": "当前节点的同级兄弟节点列表（逗号分隔），用于参考风格和内容"
            },
            {
                "name": "{{context}}",
                "description": "包含多个 Markdown 标题行的上下文文本，用于生成子节点内容"
            }
        ],
        "content": "{{context}} "
    },
    {
        "name": "生成初始树",
        "description": "以{{name}}主题为根节点，生成一个完整的多层级思维导图（保持Markdown标题结构）",
        "type": "扩展",
        "caller": "AI应用",
        "params": [
            {
                "name": "{{name}}",
                "description": "主题"
            }
        ],
        "content": "中心主题：{{name}}"
    }
]